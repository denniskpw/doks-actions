---
name: Delete k8s cluster
description: Delete a Kubernetes cluster on DigitalOcean's infrastructure

runs:
  using: docker
  image: Dockerfile

inputs:
  CLUSTER_ID:
    description: UUID of cluster to delete
    required: true
